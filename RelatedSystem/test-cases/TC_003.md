# Manual Test Cases: Welcome SMS & Reminder SMS (Orange API / AU)

**Source:** Category2 – Welcome SMS, Reminder SMS | Confirmation viewpoint: Orange API registration, service enabled: true, brand: AU  
**Viewpoint:** Reminder2  
**Template:** manual-test-cases skill (ID, Priority, Preconditions, Steps, Test Data, Postconditions)

---

## Test Case 3: Reminder2 SMS sent by next day after Welcome SMS (Admin UI – Remind2 ON)

**Test Case ID:** SMS-REMIND2-003  
**Test Case Title:** Verify Reminder2 SMS is sent by the next day after receiving Remider1 SMS when Remind2 is ON and interval is 1 day (Orange API, brand AU)  
**Priority:** High  
**Preconditions:**
- [Admin UI] **SMS Status:** Remind2 ON  
- [Admin UI] **Remind interval:** Remind2 after 1 day  
- OWL/BISCUIT conditions are met: **service enabled: true**, **brand: AU**.
- Orange API registration is enabled for the environment.
- Postman (or equivalent) is available; access to Titan DB and ISS DB for verification queries.

**Test Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | In Postman, create a new account/MDN that meets the OWL/BISCUIT conditions in advance (see Test Data for MDN requirements). | MDN/account is created with ① "使い方サポート" contract, ② Date of birth, ③ gender, ④ brand. |
| 2 | Send the POST request to the carrier enrollment API. In the request body, change only **MDN** and **SubscriberCode** as needed for the test. | Request is accepted (2xx); enrollment is registered in the system. |
| 3 | In Titan DB, run: `select * from carrier_integration.message_target mt order by created_datetime desc;` | One or more rows appear for the test MDN; SMS is scheduled (message_target records created). |
| 4 | In ISS DB, run: `select * from sms_system.message order by created_datetime desc limit 100;` | Recent records include the test MDN; messages are present for the enrollment. |
| 5 | In ISS DB (and/or Titan views as per 備考), verify that Reminder2 SMS is scheduled to be sent by the next day after Welcome SMS for that MDN. | Reminder2 SMS is scheduled and sent by the next day after receiving Welcome SMS for the test MDN. |

**Expected result (overall):** Reminder2 SMS will be sent by the next day after receiving Reminder1 SMS.

**Test Data:**

- **MDN creation requirements (when creating an MDN):**
  - ① "使い方サポート" (usage support) contract  
  - ② Date of birth  
  - ③ Gender  
  - ④ Brand  

- **POST request (curl example):**
```bash
curl --location --request POST 'https://carrier.soluto-kddi-uat.jpnmob-acyan.npr.aws.asurion.net/api/carrier/enrollment' \
--header 'Content-Type: application/json' \
--header 'X-Api-Key: 611fb6c3-882d-4da8-9172-849de6523c8c' \
--data-raw '{
  "source": "01",
  "mdn": "<change as needed>",
  "subscriberCode": "<change as needed>",
  "serviceCode": "K211",
  "startDate": "20220411",
  "subscriptionType": "01"
}'
```

- **Verification queries (備考):**
  - Titan view (realtime):  
    `SELECT * FROM titan_views.sms_system_message_realtime_view ssmrv WHERE user_id = 'c8da98db-6a11-4375-aa9e-3884104822d1' ORDER BY created_datetime DESC LIMIT 10;`
  - Titan view (mat view):  
    `SELECT * FROM titan_views.sms_system_message_mat_view ORDER BY created_datetime DESC LIMIT 10;`
  - Titan:  
    `SELECT * FROM carrier_integration.message_target mt ORDER BY created_datetime DESC;`
  - ISS:  
    `SELECT * FROM sms_system.message ORDER BY created_datetime DESC LIMIT 100;`

**Postconditions:** Test MDN has Welcome SMS and Reminder2 SMS scheduled/sent; DB rows can be used for traceability. No mandatory cleanup unless test data must be removed per project rules.

---

## Traceability

| Test Case ID   | Category              | Viewpoint | Purpose of Test   |
|----------------|-----------------------|-----------|-------------------|
| SMS-REMIND2-003 | Welcome SMS, Reminder SMS | Reminder2 | Reminder2 after 1 day (Admin UI: Remind2 ON) |

---

*Generated per manual-test-cases skill. Adjust MDN, SubscriberCode, user_id, and endpoints per environment (e.g. UAT vs production).*
