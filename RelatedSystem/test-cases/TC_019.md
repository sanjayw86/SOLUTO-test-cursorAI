# Manual Test Cases: SIGMA MEMO / Horizon SIGMA MEMO — Enrollment SMS + OpenSearch (AU)

**Source:** SIGMA MEMO >> Horizon SIGMA MEMO logs | TITAN / ISS | Enrollment SMS (CSV or Orange API)  
**Viewpoint:** Welcome / Reminder1 / Reminder2 の ISS 確認と OpenSearch（`APILog.payload.request.url` = `/carrierinterface/v1/sigma/memo`）  
**Template:** manual-test-cases skill (ID, Priority, Preconditions, Steps, Test Data, Postconditions)

---

## Test Case 19: Welcome, Reminder1, Reminder2 SMS in ISS and Horizon SIGMA MEMO logs in OpenSearch (CSV or Orange API, AU)

**Test Case ID:** SMS-SIGMA-ENROLL-HORIZON-001  
**Test Case Title:** Verify Welcome, Reminder1, and Reminder2 SMS in ISS DB after CSV or Orange API enrollment; verify Horizon SIGMA MEMO logs in OpenSearch for each (`/carrierinterface/v1/sigma/memo`)  
**Priority:** High  
**Automation ID:** C00653  
**Grade:** D  

**Preconditions:**

- OWL/BISCUIT: **service enabled: true**, **brand: AU** (adjust if testing UQ).
- Admin UI: **Remind1 ON**, **Remind2 ON**; intervals aligned with **delayInMins** or project rules.
- **CSV path:** Postman, AWS (SQA/UAT), S3 enrollment bucket, CSV format per project.
- **Orange API path:** Postman, carrier enrollment API enabled for the environment.
- Access to **Titan DB**, **ISS DB**, **ISS OpenSearch Dashboard** (Discover / Data Explorer).
- MDN / contract: ① "使い方サポート" contract, ② Date of birth, ③ Gender, ④ **Brand AU** (adjust if testing UQ).
- Know the test **Customer code** / subscriber used in enrollment (to match Horizon log payload in `APILog.payload.request.body`).

**Test Steps:**

### Phase 1 — Enrollment (CSV or Orange API) + ISS

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Prepare test MDN/contract data that meets OWL/BISCUIT (**Test Data**). | Data ready: ① 使い方サポート, ② DOB, ③ gender, ④ brand. |
| 2 | **If CSV:** GET **smsconfig**; verify `"isEnabled": true` and **delayInMins**. **If Orange API:** skip to step 6. | smsconfig OK or step skipped. |
| 3 | **If CSV:** PUT delays if needed. **If Orange API:** skip to step 6. | Updated or skipped. |
| 4 | **If CSV:** Create enrollment CSV per system format. | CSV valid for upload. |
| 5 | **If CSV:** AWS **New Enrollment** upload; Titan `carrier_enrollment` check. **If Orange API:** skip. | Uploaded / N/A. |
| 6 | **If Orange API:** POST enrollment; Titan `message_target` for MDN. **If CSV** finished 2–5: skip to 7. | 2xx + rows or skipped. |
| 7 | *(Optional)* Titan `sms_system_message` views if MDN maps to **user_id**. | Optional check. |
| 8 | Wait for CSV batch (~10 min if applicable) and Welcome / Reminder1 / Reminder2 per **delayInMins** and **11:00–18:00 JST** (**Note**). | All three in ISS when timing allows. |
| 9 | **Welcome (ISS):** In ISS DB run `select * from sms_system.message order by created_datetime desc limit 100;` locate Welcome (**Source = CARRIER ENROLLMENT**). | Welcome row present. |
| 10 | **Welcome (ISS):** Verify body vs approved **Welcome** template (product spec / ISS). | Text matches (DB/log). |
| 11 | **Welcome (ISS):** Record **Modified time**, Contract ID, customer/subscriber code. | Captured for OpenSearch. |
| 12 | **Reminder1 (ISS):** After delay, re-query ISS; locate Reminder1. | Row present. |
| 13 | **Reminder1 (ISS):** Verify body vs approved **Reminder1** template. | Text matches. |
| 14 | **Reminder1 (ISS):** Record **Modified time** and identifiers. | Captured. |
| 15 | **Reminder2 (ISS):** After delay, locate Reminder2. | Row present. |
| 16 | **Reminder2 (ISS):** Verify body vs approved **Reminder2** template. | Text matches. |
| 17 | **Reminder2 (ISS):** Record **Modified time** and identifiers. | Captured. |

### Phase 2 — OpenSearch (Horizon SIGMA MEMO)

| Step | Action | Expected Result |
|------|--------|-----------------|
| 18 | Open Discover with **Horizon SIGMA MEMO** URL (**Test Data**); adjust time, index UUID, filters. | Sigma/memo context; e.g. `APILog.payload.request.body` visible. |
| 19 | **Index pattern** `uap-acyan-api-uat*` (try `uat1` / `uat2` if no hits). | Correct index. |
| 20 | Filter **APILog.payload.request.url** = `/carrierinterface/v1/sigma/memo`. | Memo API logs only. |
| 21 | If needed: **APILog.context.user** (e.g. `asurion-uap-acyan-sqa-titan-client`). | Filter OK. |
| 22 | **Time range** covers Welcome + Reminder1 + Reminder2 (**UTC**); **Update** / **Refresh**. | Relevant hits. |
| 23 | **Horizon — Welcome:** `@timestamp` desc; open latest for Welcome; expand body. | Body visible. |
| 24 | **Horizon — Welcome:** `@timestamp` vs ISS **Modified time** (UTC vs JST ~9 h). | Aligned. |
| 25 | **Horizon — Welcome:** **Memo** = `使い方サポートSMS` (exact JSON names per sample). | Memo OK. |
| 26 | **Horizon — Welcome:** **Customer code** vs enrollment / ISS. | Matches. |
| 27 | **Horizon — Reminder1:** **Refresh** / range; repeat vs ISS Reminder1. | Aligns. |
| 28 | **Horizon — Reminder2:** **Refresh** / range; repeat vs ISS Reminder2. | All three verified. |

**Expected result (overall):** Enrollment completes via **CSV** or **Orange API**; ISS shows **Welcome**, **Reminder1**, **Reminder2** with expected bodies; OpenSearch **Horizon SIGMA MEMO** logs for `/carrierinterface/v1/sigma/memo` align on **timestamp**, **Memo** (`使い方サポートSMS`), and **Customer code**.

**Test Data:**

- **MDN creation (①–④):** ① "使い方サポート" · ② Date of birth · ③ Gender · **④ Brand AU** (adjust if testing UQ).

- **Orange API — POST example (curl):**
```bash
curl --location --request POST 'https://carrier.soluto-kddi-uat.jpnmob-acyan.npr.aws.asurion.net/api/carrier/enrollment' \
--header 'Content-Type: application/json' \
--header 'X-Api-Key: <use project key>' \
--data-raw '{
  "source": "01",
  "mdn": "<change as needed>",
  "subscriberCode": "<change as needed>",
  "serviceCode": "K211",
  "startDate": "20220411",
  "subscriptionType": "01"
}'
```

- **Titan:**  
  - CSV: `select * from carrier_integration.carrier_enrollment order by created_datetime desc;`  
  - Orange API: `select * from carrier_integration.message_target mt order by created_datetime desc;`  
  - Optional: `sms_system_message` realtime / mat views (see project)  
  - Troubleshooting: `select * from carrier_integration.request r order by created_datetime desc;` and by `contract_id`

- **ISS:** `select * from sms_system.message order by created_datetime desc limit 100;` — **Source = CARRIER ENROLLMENT** for Welcome / enrollment reminders (per project).

- **AWS / S3 (CSV path):** Portal / region / **New Enrollment** bucket per environment (SQA/UAT).

- **Horizon SIGMA MEMO — OpenSearch Discover (example — adjust time, index pattern id, APILog.context.user):**  
  `https://gk-common-oss.japan.npr.aws.asurion.net/_dashboards/app/data-explorer/discover/#?_g=(filters:!(),refreshInterval:(pause:!t,value:0),time:(from:'2026-03-19T06:00:00.000Z',to:'2026-03-19T09:00:00.000Z'))&_a=(discover:(columns:!(APILog.payload.request.body),interval:auto,sort:!(!('@timestamp',desc))),metadata:(indexPattern:'817149a0-2710-11ef-959c-0ffaed4a06ec',view:discover))&_q=(filters:!(('$state':(store:appState),meta:(alias:!n,disabled:!f,index:'9a2264c0-2710-11ef-8e62-1b550b2b5585',key:APILog.payload.request.url,negate:!f,params:(query:%2Fcarrierinterface%2Fv1%2Fsigma%2Fmemo),type:phrase),query:(match_phrase:(APILog.payload.request.url:%2Fcarrierinterface%2Fv1%2Fsigma%2Fmemo))),('$state':(store:appState),meta:(alias:!n,disabled:!f,index:'817149a0-2710-11ef-959c-0ffaed4a06ec',key:APILog.context.user,negate:!f,params:(query:asurion-uap-acyan-sqa-titan-client),type:phrase),query:(match_phrase:(APILog.context.user:asurion-uap-acyan-sqa-titan-client)))),query:(language:kuery,query:titan))`

**Note:** Use **either** CSV steps **2–5** **or** Orange API step **6** per run. **Memo** / **Customer code** field paths must match real JSON in `APILog.payload.request.body`. OpenSearch **GMT/UTC** vs ISS **JST** (~9 h per project). SMS window **11:00–18:00 JST**; ISS may show **Scheduled** then **Delivered**; OpenSearch may align with delivered time. If no hits: try `uap-acyan-api-uat1*`, `uap-acyan-api-uat2*`, filters, Customer code search, Titan **carrier_integration.request**, or escalate to FO: [Teams](https://teams.microsoft.com/l/message/19:acdbf9c190e647fbba032efc38a18146@thread.v2/1764928232546?context=%7B%22contextType%22%3A%22chat%22%7D).

**Postconditions:** Welcome, Reminder1, Reminder2 traceable in ISS; Horizon evidence captured. Cleanup per project rules only.

---

## Traceability

| Test Case ID | Category | Viewpoint | Purpose of Test |
|----------------|----------|-----------|-----------------|
| SMS-SIGMA-ENROLL-HORIZON-001 | SIGMA / Horizon SIGMA MEMO, Enrollment SMS | Welcome / Reminder1 / Reminder2 + OpenSearch | CSV or Orange API — ISS + `/carrierinterface/v1/sigma/memo` log verification |

---

*Adjust MDN, endpoints, OpenSearch index/time, and environment per project.*
