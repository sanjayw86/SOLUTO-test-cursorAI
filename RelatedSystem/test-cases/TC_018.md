# Manual Test Cases: SIGMA MEMO / ISS ATOM API — Call Reservation / Reminder / Cancellation + OpenSearch (AU)

**Source:** SIGMA Memo | TITAN / ISS >> ATOM API | Call Reservation SMS  
**Viewpoint:** Call reservation / Call reminder / Call cancellation のISSログ確認と OpenSearch（「Sending details with ATOM API」）  
**Template:** manual-test-cases skill (ID, Priority, Preconditions, Steps, Test Data, Postconditions)

---

## Test Case 18: Call Reservation, Reminder, and Cancellation SMS in ISS and SIGMA MEMO (ATOM API) logs in OpenSearch (Un-provisioned MDN, AU)

**Test Case ID:** SMS-SIGMA-CALL-ATOM-001  
**Test Case Title:** Verify Call Reservation, Call Reminder, and Call Cancellation SMS in ISS DB; verify SIGMA MEMO (ISS ATOM API) logs in OpenSearch for each  
**Priority:** High  
**Automation ID:** C00652  
**Grade:** D  
**Preconditions:**
- Call SMS conditions: **MDN un-provisioned**, **service enabled: true**, **brand: AU**.
- Test MDN available (enrolled via CSV or Orange API so the line remains un-provisioned for call reservation, per project rules).
- Access to **CC tool** and/or **Shop UI tool**, **ISS DB**, **ISS OpenSearch Dashboard**, **Titan DB** (troubleshooting).
- MDN / contract when creating: ① "使い方サポート" contract, ② Date of birth, ③ Gender, ④ Brand.
- When creating the reservation, choose a **reservation date/time** such that **Call Reminder SMS** can be sent (**before 1 hour of the actual call reservation time**) before you **cancel** the booking — otherwise reminder steps cannot be verified.

**Test Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Enroll or select a test MDN that is **un-provisioned** for call reservation, **service enabled: true**, **brand AU**. | MDN is ready for reservation in CC/Shop UI. |
| 2 | Open **CC tool** or **Shop UI tool** (URLs in Test Data). | Tool loads successfully. |
| 3 | Create a **call reservation** using the test MDN (pick a slot/time that allows **Call Reminder SMS** to be sent **before 1 hour** of the reserved call time, per product behaviour, before you cancel). | Reservation is created successfully. |
| 4 | **Call Reservation (ISS):** In ISS DB run `select * from sms_system.message order by created_datetime desc limit 10;` | Recent rows are returned. |
| 5 | **Call Reservation (ISS):** For the test MDN, locate the **Call Reservation SMS** row with **Source = SUPPORT_SESSION**. | Reservation SMS row is present. |
| 6 | **Call Reservation (ISS):** Verify the message text matches: **【使い方サポート】　お電話のご予約を承りました。ご予約情報は、使い方サポートアプリにてご確認いただけます。専用アプリはこちらからダウンロードしてください。 http://kddi-l.jp/eSd** | Text matches (confirmation via DB/log). |
| 7 | **Call Reservation (ISS):** Record **Modified time** and **metadata** (e.g. Contract ID) for this reservation SMS row. | Values captured for OpenSearch correlation. |
| 8 | **Call Reminder (ISS):** **Wait** until **Call Reminder SMS** is sent *(sent **before 1 hour** of the actual call reservation time)*. | Sufficient time has passed; Reminder SMS is expected in ISS. |
| 9 | **Call Reminder (ISS):** In ISS DB run `select * from sms_system.message order by created_datetime desc limit 10;` For the test MDN, locate the **Call Reminder SMS** row (**Source = SUPPORT_SESSION**). | Reminder SMS record is present in ISS DB. |
| 10 | **Call Reminder (ISS):** Verify the message text matches (リマインダー送信時の本文): **【使い方サポート】　お電話のご予約時間が近づいております。ご予約情報は、専用アプリにてご確認いただけます。専用アプリはこちらからダウンロードしてください。 http://kddi-l.jp/eSd　※なお、ご不在の場合はご予約がキャンセルとなる場合がございます。** | Text matches; Reminder SMS completed (確認はログ/DB). |
| 11 | **Call Reminder (ISS):** Record **Modified time** and **Contract ID** (metadata) for the **reminder** SMS row. | Values captured for OpenSearch correlation. |
| 12 | **Call Cancellation (CC/Shop):** In **CC tool** or **Shop UI**, **cancel** the reservation. | Cancellation completes successfully. |
| 13 | **Call Cancellation (ISS):** In ISS DB run `select * from sms_system.message order by created_datetime desc limit 10;` Locate **Call Cancellation SMS** with **Source = SUPPORT_SESSION** for the test MDN. | Call Cancellation SMS record is present in ISS DB. |
| 14 | **Call Cancellation (ISS):** Verify the message text matches (予約キャンセル時の本文): **【使い方サポート】　お電話のご予約がキャンセルされました。再予約をご希望の場合は、使い方サポートアプリにてご登録いただけます。（このメッセージはご不在等により自動キャンセルされた場合にも送信されます。）専用アプリはこちらからダウンロードしてください。 http://kddi-l.jp/eSd** | Text matches; cancellation SMS completed (確認はログ/DB). |
| 15 | **Call Cancellation (ISS):** Record **Modified time** and **Contract ID** (metadata) for the cancellation SMS row. | Values captured for OpenSearch correlation. |
| 16 | **OpenSearch:** Open **ISS OpenSearch Dashboard** — Discover / Data Explorer. Use the KQL query containing **"Sending details with ATOM API"** (full URL in Test Data; adjust **time from** and index pattern per environment). | Discover opens with the ATOM API search. |
| 17 | **OpenSearch:** Delete or clear obsolete **index** / **filters** if they hide results. Set the **date range** to cover the **entire test window** from **Call Reservation SMS** send time through **Call Cancellation SMS** send time (UTC-aware). Click **Update**. | Log lines for the whole period can appear (including all three SMS events). |
| 18 | **OpenSearch — Call Reservation SMS:** Identify log hit(s) that correspond to the **reservation** send. Click a **record bar**; view **Time** and **log**; expand **>** for full detail (**@timestamp**, **log**, **kubernetes.namespace_name**, etc.). | Reservation-related ATOM log detail is visible. |
| 19 | **OpenSearch — Call Reservation SMS:** Compare OpenSearch **@timestamp** with ISS **Modified time** for the **reservation** row (convert **GMT/UTC** vs **JST** per project rules, typically ~9 hours). Cross-check **Contract ID** in the log with ISS **metadata** for the reservation row. Verify **kubernetes.namespace_name** is **soluto-uat-blue** or **soluto-sqa-blue**. Review **additional** hits for the same Contract ID / subscriber (often two ATOM lines per send). | Timestamp, Contract ID, and namespace align with ISS for reservation; extra lines reviewed. |
| 20 | **OpenSearch — Call Reminder SMS:** Click **Refresh**. Adjust **date range** if the **reminder** send falls outside the current window; click **Update**. Locate hit(s) for the **reminder** send; expand **>** ; repeat the same checks against the ISS **reminder** row (**@timestamp** vs **Modified time**, **Contract ID**, **kubernetes.namespace_name**, additional hits). | Reminder ATOM logs align with ISS after conversion; namespace and extra hits reviewed. |
| 21 | **OpenSearch — Call Cancellation SMS:** Click **Refresh**. Extend **date range** to include **cancellation** send time if needed; click **Update**. Locate hit(s) for the **cancellation** send; expand **>** ; verify **@timestamp** and **Contract ID** against the ISS **cancellation** row (UTC/JST); verify **kubernetes.namespace_name** where visible; review additional hits if any. | Cancellation ATOM logs align with ISS; evidence complete for all three SMS types. |

**Expected result (overall):** **Call Reservation SMS**, **Call Reminder SMS**, and **Call Cancellation SMS** are each present in ISS with the expected **Japanese** bodies; OpenSearch shows **"Sending details with ATOM API"** logs that align with ISS for each event on **timestamp**, **Contract ID**, and **namespace** where applicable.

**Test Data:**

- **MDN creation requirements:** ① "使い方サポート" contract, ② Date of birth, ③ Gender, ④ Brand.

- **ISS:** `select * from sms_system.message order by created_datetime desc limit 10;` — **Source = SUPPORT_SESSION** for reservation / reminder / cancellation SMS.

- **Call Reservation SMS — expected Japanese text:**  
  **【使い方サポート】　お電話のご予約を承りました。ご予約情報は、使い方サポートアプリにてご確認いただけます。専用アプリはこちらからダウンロードしてください。 http://kddi-l.jp/eSd**

- **Call Reminder SMS — expected Japanese text (リマインダー):**  
  **【使い方サポート】　お電話のご予約時間が近づいております。ご予約情報は、専用アプリにてご確認いただけます。専用アプリはこちらからダウンロードしてください。 http://kddi-l.jp/eSd　※なお、ご不在の場合はご予約がキャンセルとなる場合がございます。**

- **Call Cancellation SMS — expected Japanese text (キャンセル完了後):**  
  **【使い方サポート】　お電話のご予約がキャンセルされました。再予約をご希望の場合は、使い方サポートアプリにてご登録いただけます。（このメッセージはご不在等により自動キャンセルされた場合にも送信されます。）専用アプリはこちらからダウンロードしてください。 http://kddi-l.jp/eSd**

- **Titan (troubleshooting):**  
  - `select * from carrier_integration.request r order by created_datetime desc;`  
  - `select * from carrier_integration.request where contract_id = '<contract_id>';`

- **CC Tool:** https://callcenter.soluto-kddi-sqa.jpnmob-acyan.npr.aws.asurion.net/reservation/search  
- **Shop UI Tool:** https://shop.soluto-kddi-sqa.jp.nonprod-asurion53.com/auShop-entry  

- **OpenSearch Discover (example — adjust time/index):**  
  `https://gk-common-oss.japan.npr.aws.asurion.net/_dashboards/app/data-explorer/discover/#?_g=(filters:!(),refreshInterval:(pause:!t,value:0),time:(from:'2026-01-18T02:29:02.178Z',to:now))&_a=(discover:(columns:!(log),interval:auto,sort:!()),metadata:(indexPattern:'16028b70-f054-11f0-be00-29cc124a2e2a',view:discover))&_q=(filters:!(),query:(language:kuery,query:'%22Sending%20details%20with%20ATOM%20API%22'))`

**Note:** Steps **1–15** verify ISS and CC/Shop (reservation → reminder → cancellation). Steps **16–21** verify OpenSearch after ISS is complete. In ISS, **CARRIER ENROLLMENT** is used for Welcome and enrollment reminders; **SUPPORT_SESSION** is used for reservation-related SMS (reservation, reminder, cancellation). **Call Reminder SMS** is sent **before 1 hour** of the reserved call time — plan the reservation slot and test timeline accordingly. The **Cancellation SMS** Japanese text is also sent when the reservation is **automatically cancelled** (e.g. user absent). SMS delivery window is typically **11:00–18:00 JST**; after 18:00 JST ISS may show **Scheduled** until the next delivery window, then **Delivered**; OpenSearch may show logs at delivered time. If OpenSearch has no hits, try index patterns `uap-acyan-api-uat1*`, `uap-acyan-api-uat2*`, remove filters, search by Contract ID or subscriber code, check Titan **carrier_integration.request**, or escalate to FO: [Teams](https://teams.microsoft.com/l/message/19:acdbf9c190e647fbba032efc38a18146@thread.v2/1764928232546?context=%7B%22contextType%22%3A%22chat%22%7D).

**Postconditions:** Reservation, reminder, and cancellation are traceable in ISS; OpenSearch evidence captured for each phase where logs exist. Cleanup per project rules only.

---

## Traceability

| Test Case ID | Category | Viewpoint | Purpose of Test |
|----------------|----------|-----------|-----------------|
| SMS-SIGMA-CALL-ATOM-001 | SIGMA / ATOM API, Call SMS | Reservation / Reminder / Cancellation + OpenSearch | Un-provisioned MDN — ISS + ATOM API log verification for all three SMS |

---

*Generated per manual-test-cases skill. Adjust MDN, tool URLs, OpenSearch index/time, and environment per project.*
